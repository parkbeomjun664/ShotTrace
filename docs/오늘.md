# 작업 일지

> 하루의 끝에 세 줄을 남긴다 — **한 일 · 배운 것 · 내일 할 것**.
> 1~2월 마무리 복습(R01~R03)의 원자료가 된다. 길게 쓰지 않는다.

---

## 2026-09-03 목 · W01

**한 일**
- LOT 상세 화면 완성 — 시간 구간 조인을 화면에 (`18b5b45`)
- KST 표시 버그 발견·수정, 포맷 함수를 `lib/format.ts` 한 곳으로 (`ed089fa`)
- 커리큘럼 번호를 실제 강의 번호에 맞춤 · M14 Python 신설 (`863514c`)
- 완성 목표를 12월로 당기고 주·일 단위 일정 수립 (`a811e81`)
- 코드 복습 — `server.ts` · `queries.ts` A~E · `format.ts` (`a7b95ec`)

**배운 것**
- ADR에 써놓고도 코드가 어길 수 있다. 규칙은 기억력이 아니라 **구조로** 지킨다
  → 시각 포맷을 한 파일에 모으니 어기기 어려워졌다
- `select` 문자열을 `+`로 이어붙이면 타입이 사라진다 (`GenericStringError`)
- `product_id` 조건을 빼면 조인 결과가 정확히 2배 — 패밀리 금형 (ADR 005)
- `Intl.DateTimeFormat`은 만드는 게 비싸다. 모듈 최상단에서 한 번만

**내일 할 것**
- 화면 코드 복습 — `app/page.tsx` · `app/lots/page.tsx` · `app/lots/[lotId]/page.tsx`

---

## 2026-09-07 월 · W02

**한 일**
- 화면 코드 복습 3개 — 현황판 · LOT 목록 · LOT 상세
- 세 파일 주석을 코드 오른쪽으로 정리

**배운 것**
- **마스터를 기준으로 그리고 실적을 붙인다.** 반대로 하면 안 돌아간 설비가
  화면에서 사라진다 — 현장에서 제일 위험하다
- **강조하려면 나머지를 죽여라.** 다만 줄 수에 따라 방식이 다르다
  (25줄은 0을 흐리게 / 409줄은 행 전체를 칠하기)
- `notFound()` 의 반환 타입이 `never` 라서 그 아래로 `lot` 의 `null` 이 사라진다
  → `getLotShots` 의 `NonNullable` 과 맞물려 **404 처리를 잊으면 컴파일이 안 된다**
- `loading.tsx` 가 로딩 표시를 주는 대신 404가 200으로 나간다 — 트레이드오프

**내일 할 것**
- `scripts/etl/load.py` 복습 — ETL 전체를 다시 읽는다 (코드 복습 마지막)

---

## 2026-09-08 화 · W02

**한 일**
- `load.py` 복습 (217줄) — 코드 복습 마지막
- `to_lots` 의 ADR 번호 오기 정정 (005 → 006)

**배운 것**
- **`assert` 는 가정을 코드에 박는 것.** 주석과 달리 틀리면 실제로 멈춘다
  → `_id` 고유성은 실제로 한 번 반박당한 가정이라 여기 넣었다
- **`tz_localize` 는 라벨 붙이기 / `tz_convert` 는 값 옮기기** — 다른 함수다
- **pandas 값은 그대로 못 보낸다** — `NaN`→`None`, `numpy.int64`→`int`,
  `Timestamp`→문자열. 특히 `numpy.int64` 는 눈으로 구분이 안 된다
- **크기에 따라 방식이 갈린다** — 25행은 `itertuples`, 5,232행은 벡터 연산
- **왕복을 줄여라는 ETL(배치)과 화면(N+1)에서 같은 원칙**

**내일 할 것**
- `003_indexes.sql` **직접 작성** — M06 ★. Claude는 설명과 리뷰만

